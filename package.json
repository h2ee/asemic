{
    "devDependencies": {
        "vite": "^6.2.4"
    },
    "dependencies": {
        "d3-delaunay": "^6.0.4",
        "lil-gui": "0.20.0",
        "three": "^0.175.0"
    },
    "scripts": {
        "dev": "vite",
        "bridge": "node src/dev/bridge-server.mjs",
        "build": "vite build",
        "preview": "vite preview",
        "commit": "git add . && git commit -m 'update' && git push",
        "deploy": "npm run build && git add dist -f && git commit -m 'deploy' && git subtree push --prefix dist origin gh-pages"
    }
}
